output/

# Generated scenario-plot figures (Stage 16).  Rebuilt by
# `make figures`; release.yml regenerates them with the pinned
# matplotlib before the PDF build.  The plot scripts + manifest.yaml
# are committed; the rendered PDFs are not.
figures/*.pdf

*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.synctex.gz
*.toc

# Legacy ALS-Dimmer template leftovers retained locally only.
src/als-dimmer-spec.tex
src/space6-als-dimmer-spec.tex
images/control-loop-response.jpg
images/fpga-interface-setup.png
