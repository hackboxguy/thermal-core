#ifndef _FUNCONFIG_H
#define _FUNCONFIG_H

/* thermal-core CH32V003 STANDALONE -- ch32fun build configuration.
 *
 * Empty body: the ch32fun defaults are taken as-is. The regulator
 * is headless (PRD Appendix D.5); FUNCONF_USE_DEBUGPRINTF stays at
 * its default so an optional SWIO status line is available, but the
 * control loop does not depend on it.
 */

#endif /* _FUNCONFIG_H */
