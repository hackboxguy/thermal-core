# ESP-IDF build artefacts (regenerated by `idf.py build`).
build/
sdkconfig
sdkconfig.old
dependencies.lock

# Managed components are fetched from the IDF registry on each
# build; their exact versions are pinned via idf_component.yml +
# dependencies.lock (lock file is generated, not committed).
managed_components/
