/* core/thermal_core.c — Stage 0 placeholder.
 *
 * Real implementation begins at Stage 1 (type declarations land as headers)
 * and Stage 2 (first module: curve interpolation). This stub exists so the
 * portability-guard CI job has a non-empty core archive to inspect from the
 * moment Stage 0 exits. The empty function body generates zero undefined
 * symbols, so both prongs of the no-heap-no-syscall check pass trivially.
 */
#include "thermal_config.h"

void thermal_core__stage0_placeholder(void) {
}
